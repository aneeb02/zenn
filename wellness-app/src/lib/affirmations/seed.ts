import { prisma } from '@/lib/db/prisma';

export async function seedAffirmations() {
  // Seed function is optional, affirmations are generated dynamically
  console.log('Affirmations are generated dynamically based on user profile');
  return;
}
